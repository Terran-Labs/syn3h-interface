module.exports = {
    purge: [],
    darkMode: true,
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    // @ts-ignore
    plugins: [require("@tailwindcss/forms")],
}
